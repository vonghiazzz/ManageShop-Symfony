{% extends 'base.html.twig' %}
{% block title %}Add Category | Admin{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/homepage.css') }}" />
    <link rel="stylesheet" href="{{ asset('css/manage.css') }}" />
{% endblock %}

{% block body %}
{% include 'header.html.twig' %}
    <section class="manage-section">
            <div class="container">
                <div class="row">
                    <div class="col-lg-3 col-12">
                        <ul class="nav-sidebar">
                            <li class="active"><a href="{{path('show_manage')}}">Product</a></li>
                            <li><a href="{{path('productDetail_show')}}">Product detail</a></li>
                            <li><a href="{{path('show_category')}}">Category</a></li>
                            <li><a href="{{path('brand_show')}}">Brand</a></li>
                            <li><a href="{{path('image_show')}}">Image detail</a></li>
                        </ul>
                    </div>
                    <div class="col-lg-9 col-12">
                        <div class="heading">
                            <h1 class="title-manage-info">
                                Manage Image Detail
                            </h1>
                            <a class="insert-link" href="{{path('image_insert')}}"><button class="btn btn-info">Insert</button></a>
                        </div>
                        <table class="table">
                            <thead>
                              <tr>
                                <th scope="col">#</th>
                                <th scope="col">Product</th>
                                <th scope="col">Image</th>
                                <th>Action</th>
                                <!-- <th scope="col">Handle</th> -->
                              </tr>
                            </thead>
                            <tbody>
                            {% for i in img %}
                            {% set img = i.image %}
                            <tr>
                                <th scope="row">{{i.id}}</th>
                                <td>{{i.product_name}}</td>
                                <td><img src="{{asset('uploads/'~img)}}" alt="" width="20%" height="20%"></td>
                                <td>
                                    <a href="{{path('img_edit', {'id':i.id})}}">Edit</a>
                                    <a href="{{path('img_delete', {'id':i.id})}}">Delete</a></td>
                                </td>
                            </tr>
                            {% endfor %}
                            </tbody>
                          </table>
                    </div>
                </div>
            </div>
        </section>
{% include 'footer.html.twig' %}
{% endblock %}